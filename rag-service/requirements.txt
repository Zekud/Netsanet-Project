# ───── Core RAG service dependencies (Python 3.14 compatible) ─────
# Only the packages actually used by main.py and engine.py.
# Removed strict pins that don't exist for Python 3.14 (e.g. onnxruntime==1.22.1)

# API framework
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.9

# Vector database + sentence embeddings
chromadb>=0.5.0
sentence-transformers>=2.2.0

# HTTP / async
httpx>=0.24.0
requests>=2.31.0
aiohttp>=3.8.0

# Environment variables
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0

# PDF processing (ingestion pipeline only — not needed for /ask)
pdfplumber>=0.9.0

# Optional Azure STT/TTS (comment out if no AZURE_SPEECH_KEY)
# azure-cognitiveservices-speech>=1.35.0
# azure-core>=1.26.0
# azure-storage-blob>=12.16.0
